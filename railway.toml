[build]
dockerfilePath = "generation/Dockerfile"
dockerContextDir = "generation"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
